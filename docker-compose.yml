version: "3.9"

services:
  ai-backend:
    build:
      context: ./ai_backend
    container_name: ai-backend
    ports:
      - "8001:8001"

  ui-backend:
    build:
      context: ./ui_backend
    container_name: ui-backend
    ports:
      - "8000:8000"
    depends_on:
      - ai-backend
